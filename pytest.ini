[pytest]
testpaths = tests
pythonpath = backend
markers =
    dll: Windows DLL integration (needs MatLabXRK DLL + local .xrk fixture)
    libxrk: libxrk parser integration (needs local .xrk fixture, no DLL)
addopts = -ra
